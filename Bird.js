class Bird extends BaseClass {
  constructor(x,y){
    super(x,y,50,50);
    fill("brown");
  }

  display() {
    //this.body.position.x = mouseX;
    //this.body.position.y = mouseY;
    super.display();
    
  
  }
}
